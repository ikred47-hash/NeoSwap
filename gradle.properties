# Increase memory to prevent heap space errors
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1g

# Disable Jetifier to stop the mutation crash
android.useAndroidX=true
android.enableJetifier=false
