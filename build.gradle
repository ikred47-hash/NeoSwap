buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Updated to a version compatible with newer Gradle (8.3+)
        classpath 'com.android.tools.build:gradle:8.3.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.22"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
